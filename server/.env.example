# 复制为 .env 后按需修改
PORT=5000

# 内置 MongoDB (start-all 带 --auth) 默认无需修改，db.ts 已有默认连接串
# MONGO_URI=mongodb://appuser:app123@localhost:27017/slate-mern-app?authSource=admin

# 系统 MongoDB / Windows 服务 (无认证) 时使用：
# MONGO_URI=mongodb://localhost:27017/slate-mern-app

JWT_SECRET=slate-mern-jwt-secret-dev
