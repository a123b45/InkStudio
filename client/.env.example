# 复制为 .env.local 按需修改
# 开发环境留空即可，Vite 会把 /api 代理到 localhost:5000
# VITE_API_URL=

# 生产 / Android 打包时使用（见 .env.production）
# 备案前 IP 部署：
# VITE_API_URL=http://193.112.202.161:5000
# 备案后域名：
# VITE_API_URL=https://api.inkstudio.work
