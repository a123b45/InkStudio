# 复制为 .env（start.sh 会自动创建并生成 JWT_SECRET）
DEPLOY_HOST=193.112.202.161

# start.sh 若未设置 CORS_ORIGIN 会自动追加
# CORS_ORIGIN=http://193.112.202.161:8080,http://193.112.202.161:5000,capacitor://localhost,https://localhost,http://localhost

JWT_SECRET=

# mongo-express 可视化（http://IP:8081）
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=
